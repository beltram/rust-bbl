## Borrowing 
#### Use reference or return
<img src="lib/images/ref-or-return.svg" style="height: 40vh"/>

<!--
fn do_stuff() {
    let s1: String = String::from("hello");
    salute(&s1);
    let s1 = to_deaf_lang(s1);
    to_deaf_lang(s1);
}

fn salute(hi: &String) { println!("{:?} world", hi) }

fn to_deaf_lang(to: String) -> String {
    println!("{} -> 👋", to);
    to
}-->
