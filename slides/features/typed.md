## Typed 
<img src="lib/images/typed.svg" style="height:45vh"/> 

<!--
trait Drive { fn drive(&self); }

struct Car {
    pub color: String,
    key: String,
}

impl Drive for Car {
    fn drive(&self) {
        println!("In my {} 🚗", self.color)
    }
}

impl Default for Car {
    fn default() -> Self {
        Car {
            color: "'blue".to_string(),
            key: "🔑".to_string(),
        }
    }
}-->
